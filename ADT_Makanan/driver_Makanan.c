#include "makanan.h"

int main(){
    MAKANAN m;
    IDEM id;
    NamaMakanan namaMakanan;
    TIME exp;
    TIME deliv;
    LokasiAksi loc;

    CreateMakanan(&m, id, namaMakanan, exp, deliv, loc);
}