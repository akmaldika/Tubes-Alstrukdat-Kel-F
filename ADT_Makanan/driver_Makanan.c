#include "makanan.h"

int main(){
    MAKANAN m;
    IDEM id;
    NamaMakanan namaMakanan;
    TIME exp;
    TIME TAction;
    LokasiAksi loc;

    CreateMakanan(&m, id, namaMakanan, exp, TAction, loc);
}